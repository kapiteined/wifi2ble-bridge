# /etc/default/wifi2usb-bridge-relay
# USB serial device for the MeshCore companion.
# Use "auto" to auto-detect by VID:PID and name heuristics,
# or set an explicit path like /dev/ttyACM0 or /dev/ttyUSB0.
USB_DEVICE=auto
